[tool.black]
line-length = 100
target-version = ["py311"]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
# 选择的规则集：E(pep8) F(pyflakes) I(isort)
select = ["E", "F", "I"]
ignore = []

[tool.ruff.format]
quote-style = "double"
