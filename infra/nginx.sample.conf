upstream api { server 127.0.0.1:8000; }
server {
  listen 80;
  server_name example.com;
  location / { proxy_pass http://api/; }
  location /data/ {
    alias /path/to/repo/dev_outputs/;
    autoindex on;
  }
}
